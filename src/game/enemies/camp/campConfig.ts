const bigCircleWithRand = { weaponType: "rand", size: "Big" };
export const bigCircleWithChain = { weaponType: "chain", size: "Big" };
const normalCircleWithRand = { weaponType: "rand", size: "Normal" };
const normalCircleWithChain = { weaponType: "chain", size: "Normal" };
export const campGroupComposition = [
	bigCircleWithRand,
	bigCircleWithChain,
	bigCircleWithChain,
	normalCircleWithRand,
	normalCircleWithChain
];

export const campMaxPopulation = 5;

export const numberOfBuildings = 3;
